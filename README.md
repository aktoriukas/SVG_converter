## Convert SVG's to react components

1. SVG's are in the `svg` folder
2. All SVG's has unique names
3. run 'node index.js' to convert all SVG's to react components
4. All components are in the 'components.tsx' file
